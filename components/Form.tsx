

interface FormProps {
  postId?: string
  placeholder: string
  isComment?: boolean
}


const Form = ({ postId, placeholder, isComment }: FormProps) => {
  return (
    <div>Form</div>
  )
}

export default Form